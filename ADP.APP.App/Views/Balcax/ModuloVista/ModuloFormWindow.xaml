<Window x:Class="SysAdm.Views.UserControls.ModuloFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Datos del Módulo" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Id (Solo lectura al editar)" FontSize="11" Foreground="#666666" Margin="0,0,0,2"/>
                <TextBox x:Name="txtId" IsReadOnly="True" Margin="0,0,0,10" Background="#F0F0F0" Height="24"/>

                <TextBlock Text="Nombre del Módulo *" FontSize="11" Foreground="#666666" Margin="0,0,0,2"/>
                <TextBox x:Name="txtNombre" Margin="0,0,0,10" Height="24"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnCancelar" Content="Cancelar" Width="80" Height="28" Margin="0,0,8,0" Click="BtnCancelar_Click"/>
            <Button x:Name="btnGuardar" Content="Guardar" Width="80" Height="28" Background="#0078D4" Foreground="White" Click="BtnGuardar_Click"/>
        </StackPanel>
    </Grid>
</Window>