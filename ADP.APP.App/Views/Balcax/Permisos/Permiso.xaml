<UserControl x:Class="SysAdm.Views.UserControls.Permisos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Estilo base de Botón con Icono -->
        <Style x:Key="BtnAccion" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" 
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <TextBlock FontFamily="{StaticResource MaterialDesignIcons}"
                                           Text="{TemplateBinding Tag}"
                                           FontSize="15"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnLimpiar" TargetType="Button" BasedOn="{StaticResource BtnAccion}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="BorderBrush" Value="#5A6268"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="BtnBuscar" TargetType="Button" BasedOn="{StaticResource BtnAccion}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="BtnPermiso" TargetType="Button" BasedOn="{StaticResource BtnAccion}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Búsqueda -->
            <RowDefinition Height="*"/>     <!-- Tabla DataGrid -->
            <RowDefinition Height="Auto"/>  <!-- Barra de Acciones -->
        </Grid.RowDefinitions>

        <!-- ==================== GRUPO BÚSQUEDA ==================== -->
        <GroupBox Grid.Row="0" Header="Gestión de Permisos por Trabajador / Usuario" Margin="6"
                  Padding="8,6" BorderBrush="#CCCCCC">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Buscar por:" FontSize="11" Foreground="#555555" Margin="0,0,0,2"/>
                    <ComboBox x:Name="cmbFiltro" Width="170" Height="26" SelectedIndex="0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Todos"/>
                        <ComboBoxItem Content="ID Usuario"/>
                        <ComboBoxItem Content="Código Trabajador"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Valor de búsqueda:" FontSize="11" Foreground="#555555" Margin="0,0,0,2"/>
                    <TextBox x:Name="txtBusqueda" Height="26" VerticalContentAlignment="Center" 
                             Padding="4,3" BorderBrush="#CCCCCC" KeyDown="TxtBusqueda_KeyDown"/>
                </StackPanel>

                <Button x:Name="btnLimpiar" 
                        Grid.Column="4" 
                        Tag="cleaning_services"
                        Content="Limpiar" 
                        Style="{StaticResource BtnLimpiar}"
                        VerticalAlignment="Bottom" 
                        Height="26" 
                        Width="80" 
                        Click="BtnLimpiar_Click"/>

                <Button x:Name="btnBuscar" 
                        Grid.Column="6" 
                        Tag="search"
                        Content="Buscar"
                        Style="{StaticResource BtnBuscar}"
                        VerticalAlignment="Bottom" 
                        Height="26" 
                        Width="90" 
                        Click="BtnBuscar_Click"/>
            </Grid>
        </GroupBox>

        <!-- ==================== DATAGRID DE RESULTADOS ==================== -->
        <DataGrid x:Name="dgvTrabajadoresUsuarios" Grid.Row="1"
                  Margin="6,0,6,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="All"
                  RowHeaderWidth="35"
                  LoadingRow="dgvTrabajadoresUsuarios_LoadingRow"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#F7F9FC"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">

            <DataGrid.RowHeaderStyle>
                <Style TargetType="DataGridRowHeader">
                    <Setter Property="Width" Value="35"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Background" Value="#F0F2F5"/>
                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="Foreground" Value="#555555"/>
                </Style>
            </DataGrid.RowHeaderStyle>

            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#E2EDF8"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#333333"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#EAEAEA"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#333333"/>
            </DataGrid.Resources>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F0F0F0"/>
                    <Setter Property="Foreground" Value="#333333"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Usuario" Binding="{Binding IdUsuario}" Width="80"/>
                <DataGridTextColumn Header="Cód. Trabajador" Binding="{Binding CodigoTrabajador}" Width="110"/>
                <DataGridTextColumn Header="Nombre Completo" Binding="{Binding NombreCompleto}" Width="220"/>
                <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="200"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="110"/>
                <DataGridCheckBoxColumn Header="Habilitado" Binding="{Binding Habilitado}" Width="80">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
                <DataGridTextColumn Header="Puesto" Binding="{Binding NombrePuesto}" Width="150"/>
                <DataGridTextColumn Header="Departamento" Binding="{Binding NombreDepartamento}" Width="150"/>
                <DataGridTextColumn Header="Gerencia" Binding="{Binding NombreGerencia}" Width="150"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding NombreDireccion}" Width="150"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                <DataGridTextColumn Header="Tipo Empleado" Binding="{Binding TipoEmpleado}" Width="110"/>
                <DataGridTextColumn Header="F. Ingreso" Binding="{Binding FechaIngreso, StringFormat='\{0:yyyy-MM-dd\}'}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ==================== BARRA DE ACCIONES ==================== -->
        <Border Grid.Row="2" Background="#F5F6F8" BorderBrush="#DDDDDD"
                BorderThickness="0,1,0,0" Padding="8" Margin="6">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="Acciones:" FontWeight="SemiBold"
                               VerticalAlignment="Center" FontSize="12"
                               Foreground="#333333" Margin="0,0,10,0"/>

                    <!-- Botón Permiso -->
                    <Button x:Name="btnPermiso"
                            Style="{StaticResource BtnPermiso}"
                            Tag="vpn_key"
                            Content="Permiso a Vista"
                            Width="160"
                            Click="BtnPermiso_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>